// Static type -> Statically-type
// variables value cannot be changed in the future
// Example: string nane ='Vusi';
// 'Vusi' value can never be changed at runtime

// Dynamic type - Javascript is a dynamic language -> (Dynamically-typed)
// Variables value can be changed in th future or at runtime
// let name = 'Vusi';

let name = "Vusi";
let age = 30;
let isApproved = false;
let firstName = undefined;
let lastName = null;
let selectedColor = null;

console.log(typeof name); // String
console.log(typeof age); // Number
console.log(typeof isApproved); // Boolean
console.log(typeof firstName); // undefined
console.log(typeof selectedColor); // object
