// Types of Functions

// Performing a task
function greet(firstName, lastName) {
  console.log("Hello " + firstName + " " + lastName);
}

// Calculate a value
function square(number) {
  return number * number;
}

greet("John", "Smith");

console.log(square(2)); // 2 function are executed at log() and square(2)
